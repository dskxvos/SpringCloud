server:
  port: 8762
spring:
  application:
    name: eureka-client-hi
eureka:
  instance:
      lease-expiration-duration-in-seconds: 30 #多久没有上报心跳就移除自己(S)
      lease-renewal-interval-in-seconds: 12  #心跳间隔时间(S)
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
